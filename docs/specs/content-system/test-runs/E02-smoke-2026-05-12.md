---
title: E02 Smoke E2E — 2026-05-12
type: test-run
status: PASS
date: 2026-05-12
operator: Zion (autônomo, sob autorização do Kaique)
target: case-refs / Supabase project knusqfbvhsqworzyhvip
---

# E02 Smoke E2E — 2026-05-12 PASS

Validação fim-a-fim do fluxo de curadoria editorial implementado em E02. Rodado via `curl` direto contra a Edge Function `case-refs-mutate` + REST PostgREST. Item de teste: `id=44`, perfil `marinacristofani` (item legado pré-E02, escolhido por estar com `promoted_at IS NULL` no momento do teste).

## Resultado

**PASS — 6/6 steps**

| # | Step | Esperado | Recebido |
|---|------|----------|----------|
| 1 | POST `promote_editorial` sem campos | 422 | 422 `{error:"missing_editorial_fields",fields:["quando_usar","por_que_funciona","como_adaptar"]}` |
| 2 | POST `promote_editorial` com `<20` chars | 422 | 422 mesma payload do step 1 |
| 3 | POST `promote_editorial` válido | 200 + promoted_at | 200 `{ok:true,op,data:[{out_id:44,out_promoted_at:"2026-05-12T22:55:10..."}]}` |
| 4 | GET `v_referencias_promovidas?id=eq.44` | 1 linha c/ 3 campos | 1 linha c/ `quando_usar`/`por_que_funciona`/`como_adaptar` populados |
| 5 | POST `unpromote` | 200 | 200 `{ok:true,op:"unpromote",data:[{id:44}]}` |
| 6 | GET `v_referencias_publicas?id=eq.44` | `promoted_at:null` | `promoted_at:null` |

## Observações

1. **Estado pós-teste**: o item id=44 ficou com os 3 campos editoriais **preenchidos** em estado de rascunho (`promoted_at=null` mas `quando_usar/por_que_funciona/como_adaptar` populados). Isso é comportamento intencional — a constraint só ativa quando `promoted_at IS NOT NULL`, então rascunhos sobrevivem ao unpromote. Se Kaique reabrir o item no `/live`, vai ver o texto do teste como pré-preenchimento das textareas. Edição manual via UI sobrescreve.

2. **Bugs cosméticos detectados durante o smoke + corrigidos**:
   - `op: 'promote'` legacy ainda existia na Edge Function mas a CHECK constraint `chk_promoted_requires_editorial_fields` quebrava qualquer chamada → branch removida.
   - `{ ok, op, ...r.data }` (spread de array) produzia `{0:{...}}` no response → trocado por `{ ok, op, data: r.data }`.
   - RPC `case_refs_promote_editorial` declarava `id INT` mas tabela é `BIGSERIAL` → recriada com `BIGINT` + alias `out_id/out_promoted_at`.

3. **Smoke executado via cURL direto** — sem usar a UI do `/live`. Validação da UI fica como teste manual posterior. Como o fluxo backend está validado, a UI (que só envia o mesmo POST) deve funcionar identicamente.

## Comandos rodados (reproduzíveis)

```bash
ANON='<anon_key>'
ENDPOINT='https://knusqfbvhsqworzyhvip.supabase.co/functions/v1/case-refs-mutate'

# Step 1 — 422
curl -X POST "$ENDPOINT" -H "apikey: $ANON" -H "Authorization: Bearer $ANON" \
  -H "Content-Type: application/json" \
  -d '{"op":"promote_editorial","id":44}'

# Step 3 — 200
curl -X POST "$ENDPOINT" -H "apikey: $ANON" -H "Authorization: Bearer $ANON" \
  -H "Content-Type: application/json" \
  -d '{"op":"promote_editorial","id":44,"quando_usar":"...","por_que_funciona":"...","como_adaptar":"..."}'
```

## Próximos passos

- Marcar E02 como Done.
- Marcar E01-S4 (guia-decida.md) e E03-S3 (exemplos) como Done — Queila optou por não revisar.
- Avançar pra E04-E08 (4 agentes editoriais + rollout).
